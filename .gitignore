# ========================================================================
# .NET / Visual Studio / Rider
# ========================================================================
[Bb]in/
[Oo]bj/
[Oo]ut/
[Dd]ebug*/
[Rr]elease*/
[Ll]og/
[Ll]ogs/
*.user
*.userprefs
*.suo
*.userosscache
*.sln.docstates
*.swp
*.cache
*.DotSettings.user
*.rsp
project.lock.json
project.fragment.lock.json
artifacts/

# Visual Studio + VS Code
.vs/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
*.code-workspace

# Rider / IntelliJ
.idea/
*.iml

# Test / coverage
[Tt]est[Rr]esult*/
*.coverage
*.coveragexml
coverage/
*.opencover.xml

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
!**/[Pp]ackages/build/
*.nuget.props
*.nuget.targets
.paket/

# Publish / deployment
[Pp]ublish/
PublishProfiles/

# Roslyn / build server
.roslyn/
.build/

# ========================================================================
# Node / Vite (root-level — projects-mfe has its own .gitignore too)
# ========================================================================
node_modules/
dist/
dist-ssr/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache

# ========================================================================
# Application data — SQLite is created at runtime by the seeder
# ========================================================================
# The DB and Identity tables are populated on first run by DbSeeder.
# Drop the file to reset to seeded state.
app.db
app.db-shm
app.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# ========================================================================
# Logs
# ========================================================================
*.log
logs/

# ========================================================================
# Secrets — never commit
# ========================================================================
# The POC's "shared HMAC key" lives in committed appsettings.json on
# purpose (it's a placeholder); real secrets go in user-secrets or a vault.
appsettings.*.local.json
appsettings.Production.json
secrets.json
*.pfx
*.p12
.env
.env.local
.env.development
.env.production

# ========================================================================
# OS noise
# ========================================================================
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ========================================================================
# Local-only Claude / agent state
# ========================================================================
.claude/
